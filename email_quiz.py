# Each student wants to create an email address.
# The email address should be the student’s name @example.com
# Print the email address for each student

names = ['Benjamin', 'Allison', 'Philip', 'Dana', 'Emily']

# for index in range(len(names)):
#     print(names[index] + '@example.com')

# or

for name in names:
    print(name + '@example.com')
